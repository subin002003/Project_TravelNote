<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="kr.co.iei.foreignPlan.model.dao.ForeignPlanDao">

	<select id="selectRegionList" resultType="foreignRegion">
		select reg.*, time_zone, currency_code from
			(select * from (select rownum as rnum, r.* from
				(select * from region where country_name != '한국' order by region_no desc)r)
				where rnum between #{startNum} and #{endNum})reg
		left join region_info on reg.region_no = region_info.region_no
	</select>
</mapper>
